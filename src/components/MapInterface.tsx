import React, { useState } from "react";
import {
  IonGrid,
  IonCol,
  IonRow,
} from "@ionic/react"

const MapInterface: React.FC<{
}> = (props) => {

  return (
    <IonGrid>
      <IonRow>
        <IonCol>
        </IonCol>
      </IonRow>
    </IonGrid>
  );
};
export default MapInterface;
